---
- include: wall.yml

# Set hostname to inventory_hostname
- include: hostname.yml

# Generate custom motd file
- include: motd.yml

# Generate /etc/hosts file
- include: hosts.yml

- include: upgrade.yml

- include: fedora-install.yml
  when: not is_atomic and ansible_distribution == "Fedora"

- include: firewall.yml
  when: replace_firewalld_with_iptables
