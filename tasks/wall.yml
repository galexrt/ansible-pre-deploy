---
- name: Send out ansible deployment started broadcast message
  shell: |
    echo '+---------------------------------------+
    | => ANSIBLE DEPLOYMENT HAS STARTED! <= |
    +---------------------------------------+' | wall
  ignore_errors: true
